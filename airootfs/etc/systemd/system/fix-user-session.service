[Unit]
Description=Fix user session permissions for Hyprland
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'chown -R flame:flame /home/flame && chmod -R 755 /home/flame/.config'
RemainAfterExit=yes

[Install]
WantedBy=graphical.target
